<?php
// nouveau.php — Formulaire Nouveau Client (style identique à index)
?>
<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <title>SHAMTEK — Nouveau Client</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="style.css">
</head>
<body>

  <!-- 🔹 HEADER (même structure que index) -->
  <header>
    <div class="container header-row">
      <div class="logo">
        <a href="/"><img src="images/logo.svg" alt="SHAMTEK" class="logo-img"></a>
      </div>
      <div class="login-icon">
        <a href="index.php" title="Accueil">
          <img src="images/login.svg" alt="Accueil" class="icon-login">
          <span class="login-text"> Accueil </span>
        </a>
      </div>
    </div>
  </header>

  <main>
    <div class="container">
      <div class="panel">
        <div class="panel-body">
          <h1 class="h1">Créer un nouveau client</h1>
          <div class="badges">
            <span class="badge">Tous les champs sont obligatoires</span>
            <span class="badge">Email valide requis</span>
          </div>

          <!-- Par défaut du TP: method='get'; vous pouvez basculer en 'post' quand vous voulez -->
          <form action="enregistrement.php" method="post" novalidate>
            <div class="grid" style="grid-template-columns: repeat(auto-fit,minmax(260px,1fr));">
              <label class="panel-body" style="padding:0">
                <div class="card-body" style="padding:0 0 6px; font-weight:600">Nom</div>
                <input type="text" name="n" required
                       value="<?php echo isset($_GET['n']) ? htmlspecialchars($_GET['n'], ENT_QUOTES, 'UTF-8') : '';?>"
                       style="width:100%; padding:10px; border:1px solid #e5e7eb; border-radius:8px;">
              </label>

              <label class="panel-body" style="padding:0">
                <div class="card-body" style="padding:0 0 6px; font-weight:600">Prénom</div>
                <input type="text" name="p" required
                       value="<?php echo isset($_GET['p']) ? htmlspecialchars($_GET['p'], ENT_QUOTES, 'UTF-8') : '';?>"
                       style="width:100%; padding:10px; border:1px solid #e5e7eb; border-radius:8px;">
              </label>

              <label class="panel-body" style="padding:0">
                <div class="card-body" style="padding:0 0 6px; font-weight:600">Adresse</div>
                <input type="text" name="adr" required
                       value="<?php echo isset($_GET['adr']) ? htmlspecialchars($_GET['adr'], ENT_QUOTES, 'UTF-8') : '';?>"
                       style="width:100%; padding:10px; border:1px solid #e5e7eb; border-radius:8px;">
              </label>

              <label class="panel-body" style="padding:0">
                <div class="card-body" style="padding:0 0 6px; font-weight:600">Numéro de téléphone</div>
                <input type="text" name="num" required
                       pattern="[0-9+ ]{6,}"
                       value="<?php echo isset($_GET['num']) ? htmlspecialchars($_GET['num'], ENT_QUOTES, 'UTF-8') : '';?>"
                       style="width:100%; padding:10px; border:1px solid #e5e7eb; border-radius:8px;">
              </label>

              <label class="panel-body" style="padding:0">
                <div class="card-body" style="padding:0 0 6px; font-weight:600">Adresse e-mail</div>
                <input type="email" name="mail" required
                       value="<?php echo isset($_GET['mail']) ? htmlspecialchars($_GET['mail'], ENT_QUOTES, 'UTF-8') : '';?>"
                       style="width:100%; padding:10px; border:1px solid #e5e7eb; border-radius:8px;">
              </label>

              <label class="panel-body" style="padding:0">
                <div class="card-body" style="padding:0 0 6px; font-weight:600">Mot de passe</div>
                <input type="password" name="mdp1" required minlength="4"
                       style="width:100%; padding:10px; border:1px solid #e5e7eb; border-radius:8px;">
              </label>

              <label class="panel-body" style="padding:0">
                <div class="card-body" style="padding:0 0 6px; font-weight:600">Confirmer votre mot de passe</div>
                <input type="password" name="mdp2" required minlength="4"
                       style="width:100%; padding:10px; border:1px solid #e5e7eb; border-radius:8px;">
              </label>
            </div>

            <div style="margin-top:14px; display:flex; gap:10px; align-items:center;">
              <button class="btn" type="submit">Valider</button>
              <a href="index.php" class="back">← Retour à l'accueil</a>
            </div>
          </form>

          </p>
        </div>
      </div>
    </div>
  </main>

 <!-- 🔹 FOOTER -->
<footer>
  <div class="container foot">
    <span>© <?= date('Y') ?> SHAMTEK</span>
    <span><a href="contact.php" class="footer-link">Contact</a></span>
  </div>
</footer>


</body>
</html>
